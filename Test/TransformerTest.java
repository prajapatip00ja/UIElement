import iosElement.IosGroup;
import org.junit.Test;
import xmlElement.Button;
import xmlElement.Label;
import xmlElement.XmlGroup;

import static junit.framework.Assert.assertEquals;

public class TransformerTest {
//    @Test
//    public void testTransformShouldTransformXmlElementToIosElement(){
//        XmlGroup xmlGroup = new XmlGroup("g1");
//        xmlGroup.addElement(new Label("lb1", "hello"));
//        xmlGroup.addElement(new Button("b1", "Hello"));
//        Transformer transformer = new Transformer();
//        IosGroup group = transformer.transform(xmlGroup);
//        String expected = "g1\n" +"  lb1  hello\n" +"  b1 Hello\n";
//        assertEquals(expected,group.toString());
//    }
}
